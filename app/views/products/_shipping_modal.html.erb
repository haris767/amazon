<div class="grid " style="width: 300px;">
    <!-- Review Stats -->
    <section aria-labelledby="delivery-stats" class="col-span-2 pt-10 pb-10">
     
    <button data-action="click->modal#toggleModal" type="button" class="relative rounded-md bg-white text-gray-400 hover:text-gray-500 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
    <span class="absolute -inset-2.5"></span>
    <span class="sr-only">Close panel</span>
    <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
      <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
    </svg>
  </button>
  <span class="flex" id="product-price">Product Price&nbsp;<%= @product.price %></span>
  <div id="shipping_modal" class="modal">
  <p class="flex">Shipping Cost: <span id="shipping-cost">0</span></p> 
  <p class="flex">Total Price: <span id="total-price"><%= @product.price %></span></p> 
</div>
</section>
  </div>